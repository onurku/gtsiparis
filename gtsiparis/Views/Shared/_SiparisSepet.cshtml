@if (Request.IsAuthenticated)
{
    <button type="button" class="btn btn-primary" onclick="location.href='@Url.Action("SiparisleriGetir", "Genel")'" >Sepet <span class="badge">@ViewBag.Miktar</span></button>
}

